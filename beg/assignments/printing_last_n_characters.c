#include<stdio.h>
#include<string.h>
int main()
{
	int num,len,p,i;
	char str[100];
    scanf("%s",str);
	scanf("%d",&num);
	len=strlen(str);
	p=l-num;
	for(i=p;i<l;i++)
	{
		printf("%c",str[i]);
	}
	return(0);	
}
